{
  "name": "a2ui-vue",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build:lib": "pnpm -r --stream run --if-present build:lib",
    "dev:restaurant": "pnpm -r --stream run --if-present --parallel dev:restaurant",
    "dev:contact": "pnpm -r --stream run --if-present --parallel dev:contact"
  },
  "keywords": [],
  "author": "",
  "license": "ISC"
}
